# TODOS

## 公众号排版工具

- [ ] 第二步：接入站内发布，把文章保存到 `jinzhe.io/blog`。
- [ ] 第二步：站内发布时把图片上传到 Cloudflare R2，Markdown 里使用站内可访问 URL。
- [ ] 第二步：给发布接口加权限保护，避免公开页面被外部写入内容。
- [ ] 第二步：发布前生成 frontmatter，包括 `title`、`description`、`pubDate`、`tags` 和 `draft`。
